using Microsoft.UI.Xaml.Controls;

namespace MeetingLive_App.Controls;

/// <summary>Claude Code logo as a self-contained, reusable control (see ClaudeCodeIcon.xaml).</summary>
public sealed partial class ClaudeCodeIcon : UserControl
{
    public ClaudeCodeIcon() => InitializeComponent();
}
