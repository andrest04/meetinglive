using Microsoft.UI.Xaml.Controls;

namespace MeetingLive_App.Controls;

/// <summary>Codex logo as a self-contained, reusable control (see CodexIcon.xaml).</summary>
public sealed partial class CodexIcon : UserControl
{
    public CodexIcon() => InitializeComponent();
}
