using Microsoft.UI.Xaml.Controls;

namespace MeetingLive_App.Controls;

/// <summary>TypeSafe (Jev) logo as a self-contained, reusable control (see TypeSafeIcon.xaml).</summary>
public sealed partial class TypeSafeIcon : UserControl
{
    public TypeSafeIcon() => InitializeComponent();
}
