using Microsoft.UI.Xaml.Controls;

namespace MeetingLive_App.Controls;

/// <summary>Grok (xAI) logo as a self-contained, reusable control (see GrokIcon.xaml).</summary>
public sealed partial class GrokIcon : UserControl
{
    public GrokIcon() => InitializeComponent();
}
