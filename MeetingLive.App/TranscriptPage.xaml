<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="MeetingLive_App.TranscriptPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="using:MeetingLive_App"
    mc:Ignorable="d">

    <Grid Padding="24" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="{x:Bind ViewModel.Title, Mode=OneWay}"
                       Style="{StaticResource BodyStrongTextBlockStyle}" Opacity="0.8" VerticalAlignment="Center" />
            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8"
                        Visibility="{x:Bind local:TranscriptPage.BoolToVisibility(ViewModel.HasContent), Mode=OneWay}">
                <Button x:Uid="TranscriptPage_Copy" Command="{x:Bind ViewModel.CopyToClipboardCommand}"
                        AutomationProperties.AutomationId="BtnCopyTranscript" />
                <Button x:Uid="TranscriptPage_OpenLocation" Command="{x:Bind ViewModel.OpenFileLocationCommand}"
                        AutomationProperties.AutomationId="BtnOpenTranscriptLocation" />
            </StackPanel>
        </Grid>

        <InfoBar Grid.Row="1"
                 x:Uid="TranscriptPage_CopyConfirmation"
                 Severity="Success"
                 IsOpen="{x:Bind ViewModel.IsCopyConfirmationOpen, Mode=TwoWay}"
                 AutomationProperties.AutomationId="InfoCopyTranscript" />

        <Grid Grid.Row="2">
            <ProgressRing IsActive="{x:Bind ViewModel.IsLoading, Mode=OneWay}"
                          HorizontalAlignment="Center" VerticalAlignment="Center" Width="32" Height="32" />

            <StackPanel Spacing="12"
                        HorizontalAlignment="Center" VerticalAlignment="Center"
                        Visibility="{x:Bind local:TranscriptPage.BoolToVisibility(ViewModel.IsEmpty), Mode=OneWay}">
                <TextBlock x:Uid="TranscriptPage_Empty"
                           Opacity="0.7" HorizontalAlignment="Center" TextAlignment="Center" />
                <Button x:Uid="TranscriptPage_EmptyCta"
                        Style="{StaticResource AccentButtonStyle}"
                        HorizontalAlignment="Center"
                        Click="EmptyCta_Click"
                        AutomationProperties.AutomationId="BtnTranscriptEmptyRecord" />
            </StackPanel>

            <ScrollViewer Visibility="{x:Bind local:TranscriptPage.BoolToVisibility(ViewModel.HasContent), Mode=OneWay}"
                          HorizontalScrollBarVisibility="Disabled">
                <TextBlock Text="{x:Bind ViewModel.Transcript, Mode=OneWay}" TextWrapping="Wrap"
                           IsTextSelectionEnabled="True"
                           AutomationProperties.AutomationId="TxtTranscriptContent" />
            </ScrollViewer>
        </Grid>
    </Grid>
</Page>
