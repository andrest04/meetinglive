# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

See [`AGENTS.md`](./AGENTS.md) — the canonical, tool-agnostic instructions for this repo (hard gate: every change must load matching skills from `.atl/skill-registry.md`, plus build/test commands and architecture). Read and follow it in full; this file exists only so Claude Code's auto-discovery finds it.
