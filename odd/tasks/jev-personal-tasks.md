# Ask tab: what do I need to do

Reshape Ask from detective search into personal task list. Optional topic (e.g. TV1). Jev picks request/commitment lines; the selected summary LLM writes the checklist. If the topic was not discussed, do not call the LLM.
